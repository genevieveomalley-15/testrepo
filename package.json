{
  "name": "slack-product-request",
  "version": "1.0.0",
  "description": "Slack global shortcut that collects product requests and creates a record in Salesforce.",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node --watch src/app.js"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.22.0",
    "dotenv": "^16.4.5"
  }
}
